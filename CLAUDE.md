# SqlSchemaMcp

This repository uses [AGENTS.md](AGENTS.md) as the canonical shared agent instructions file.

Claude-specific global defaults from `~/.claude/CLAUDE.md` still apply, but project-specific
rules and overrides live in `AGENTS.md`. Do not duplicate project instructions here.
